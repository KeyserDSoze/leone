Audio files expected by the quiz projector:

```txt
cervelleone-start.mp3
cervellone-countdown.mp3
```

The host button "Lancia jingle CervelLeone" triggers `cervelleone-start.mp3`.
The projector plays `cervellone-countdown.mp3` when a question becomes visible.
