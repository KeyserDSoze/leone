---
// Homepage con background e navigazione
import { createUrl } from '../utils/urls';
---

<!DOCTYPE html>
<html lang="it">
<head>
	<meta charset="UTF-8">
	<meta name="viewport" content="width=device-width, initial-scale=1.0">
	<title>Benvenuto Leone</title>
	<link rel="icon" type="image/svg+xml" href="/favicon.svg">
	<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.2/css/all.min.css" />
	<style is:global>
		@font-face {
  font-family: 'Playfull Rocket';
  src: url('/fonts/playfull_rocket.ttf') format('truetype');
  font-display: swap;
}
body {
  font-family: system-ui, Arial, sans-serif, "Apple Color Emoji", "Segoe UI Emoji", "Noto Color Emoji";
}
h1 {
  font-family: 'Playfull Rocket', system-ui, Arial, sans-serif;
}
		* {
			margin: 0;
			padding: 0;
			box-sizing: border-box;
		}

		body {
			height: 100vh;
			background: url('images/background.png') center center/cover no-repeat;
			display: flex;
			flex-direction: column;
			justify-content: center;
			align-items: center;
			font-family: system-ui, Arial, sans-serif;
			position: relative;
		}

		/* Overlay per migliorare la leggibilità */
		body::before {
			content: '';
			position: absolute;
			top: 0;
			left: 0;
			width: 100%;
			height: 100%;
			background: rgba(0, 0, 0, 0.3);
			z-index: 1;
		}

		.content {
			text-align: center;
			z-index: 2;
			position: relative;
			padding: 2rem;
			max-width: 600px;
		}

		h1 {
			font-size: 3rem;
			color: white;
			margin-bottom: 1rem;
			text-shadow: 2px 2px 4px rgba(0, 0, 0, 0.7);
			font-weight: bold;
		}

		.subtitle {
			font-size: 1.5rem;
			color: #ffeb3b;
			margin-bottom: 3rem;
			text-shadow: 1px 1px 2px rgba(0, 0, 0, 0.7);
		}

		.buttons-container {
			display: flex;
			flex-direction: column;
			gap: 1.5rem;
			align-items: center;
		}

		.btn {
			padding: 1.2rem 2.5rem;
			font-size: 1.3rem;
			font-weight: bold;
			text-decoration: none;
			border-radius: 2rem;
			transition: all 0.3s ease;
			display: flex;
			align-items: center;
			gap: 1rem;
			min-width: 360px;
			justify-content: center;
			box-shadow: 0 4px 15px rgba(0, 0, 0, 0.3);
		}

		.btn-lista {
			background: linear-gradient(135deg, #388e3c, #4caf50);
			color: white;
		}

		.btn-lista:hover {
			background: linear-gradient(135deg, #2e7d32, #388e3c);
			transform: translateY(-3px);
			box-shadow: 0 6px 20px rgba(0, 0, 0, 0.4);
		}

		.btn-gioco {
			background: linear-gradient(135deg, #ff6b35, #f7931e);
			color: white;
		}

		.btn-gioco:hover {
			background: linear-gradient(135deg, #e55a2b, #ff6b35);
			transform: translateY(-3px);
			box-shadow: 0 6px 20px rgba(0, 0, 0, 0.4);
		}

		.btn-tempo {
			background: linear-gradient(135deg, #8e24aa, #ba68c8);
			color: white;
		}

		.btn-tempo:hover {
			background: linear-gradient(135deg, #7b1fa2, #8e24aa);
			transform: translateY(-3px);
			box-shadow: 0 6px 20px rgba(0, 0, 0, 0.4);
		}

		.btn-storia {
			background: linear-gradient(135deg, #2196f3, #64b5f6);
			color: white;
		}

		.btn-storia:hover {
			background: linear-gradient(135deg, #1976d2, #2196f3);
			transform: translateY(-3px);
			box-shadow: 0 6px 20px rgba(0, 0, 0, 0.4);
		}

		.btn i {
			font-size: 1.5rem;
		}

		/* Responsive */
		@media (max-width: 768px) {
			h1 {
				font-size: 2.2rem;
			}
			
			.subtitle {
				font-size: 1.2rem;
			}
			
			.btn {
				min-width: 260px;
				font-size: 1.1rem;
				padding: 1rem 2rem;
			}
			
			.content {
				padding: 1rem;
			}
		}

		@media (max-width: 480px) {
			.buttons-container {
				width: 100%;
			}
			
			.btn {
				min-width: 90%;
			}
		}
	</style>
</head>
<body>
	<div class="content">
		<h1>Welcome to the jungle, Leone!</h1>
		<p class="subtitle">Pronti a divertirvi con lui?</p>
		
		<div class="buttons-container">
			<a href={createUrl("/lista-nascita")} class="btn btn-lista">
				<i class="fa-solid fa-gift"></i>
				Lista Nascita
			</a>
			
			<a href={createUrl("/mini-gioco")} class="btn btn-gioco">
				<i class="fa-solid fa-gamepad"></i>
				Gioca con Leone
			</a>
			
			<a href={createUrl("/scatola-tempo")} class="btn btn-tempo">
				<i class="fa-solid fa-hourglass-half"></i>
				Scatola del Tempo
			</a>
			
			<a href="https://narrarium.com/it/series/leone/" class="btn btn-storia">
				<i class="fa-solid fa-book-open"></i>
				Leggi una storia
			</a>
			
		</div>
	</div>
</body>
</html>
